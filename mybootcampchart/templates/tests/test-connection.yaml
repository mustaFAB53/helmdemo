apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mybootcampchart.fullname" . }}-test-connection"
  labels:
    {{- include "mybootcampchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mybootcampchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
