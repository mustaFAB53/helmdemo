apiVersion: v1
kind: Service
metadata:
  name: {{ include "mybootcampchart.fullname" . }}
  labels:
    {{- include "mybootcampchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mybootcampchart.selectorLabels" . | nindent 4 }}
